const str = "abcabcbbb"
const longestSubstr = str => {
  const hash = {}
  let startIdx = 0
  let maxLength = 0

  for (let i = 0; i < str.length; i++) {
    if (hash[str[i]] > startIdx) {
    }
  }
}

longestSubstr(str)
